"""Dashboard contract adapter modules.

Adapters normalize deterministic backend payloads into explicit dashboard
contracts. They must not import renderers, browser state, generated artifacts,
or LLM modules.
"""

